spring:
  profiles:
    active: dev

#Setting Open API path
springdoc:
  api-docs:
    path: /api-docs

#Expose all actuator endpoints
management:
  endpoints:
    web:
      exposure:
        include: '*'

movies-api:
  version: 2.0